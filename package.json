{
  "name": "smart-home-automation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Smart home automation and security product discovery website powered by Google Sheets and static JSON builds.",
  "scripts": {
    "build:data": "node scripts/build-json.mjs",
    "build:site": "node scripts/build-site.mjs",
    "build": "npm run build:data && npm run build:site",
    "lint": "node --check scripts/build-json.mjs && node --check scripts/build-site.mjs",
    "preview": "npx serve public",
    "dev": "npx serve ."
  },
  "engines": {
    "node": ">=24"
  }
}
